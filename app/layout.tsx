import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dostoevsky AI Philosopher | The Grand Inquisitor Chatbot",
  description: "Explore existential philosophy through AI conversations about Dostoevsky's 'The Grand Inquisitor'. Discuss suffering, freedom, faith, and the human condition.",
  keywords: "Dostoevsky, philosophy, AI chatbot, Grand Inquisitor, existentialism, faith, suffering, freedom",
  authors: [{ name: "Philosophy AI" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
